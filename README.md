# Rust SEP Admission Exercise

Run the following command in the terminal in this folder for further instructions. 
```bash
cargo run
```
If this does not work, check your [Rust](https://www.rust-lang.org/) installation.

Run `git init` inside this folder to add a git repo to this project.
